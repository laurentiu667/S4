-- 1.1
SELECT * FROM employe
-- 1.2
SELECT nom, prenom, departement FROM employe;
-- 1.3
SELECT departement, prenom, nom FROM employe
-- 1.4 
SELECT nom, prenom FROM employe WHERE departement = 'ventes'
-- 1.5
SELECT
    nom AS "Nom de l employé",
    prenom AS "Prénom de l employé",
    salaire AS "Salaire de l employé"
FROM
    employe
WHERE
    salaire > 20.0;
-- 1.6
SELECT nom, prenom FROM employe WHERE departement = 'ventes' AND salaire > 20;
-- 1.7
SELECT nom, prenom FROM employe WHERE departement = 'r&d' AND ville = 'Montréal' AND salaire > 20;
-- 1.8
-- A
SELECT nom, prenom, ville FROM employe WHERE departement = 'r&d' AND (ville = 'Laval' OR ville = 'Longueuil');
-- B
SELECT nom, prenom, ville FROM employe WHERE departement = 'r&d' AND ville IN ('Laval', 'Longueuil');
-- 1.9
SELECT nom, prenom, ville FROM employe WHERE superviseur = 111;
-- 1.10
-- A
SELECT nom, prenom, departement, salaire FROM employe WHERE salaire >= 20 AND salaire <= 30;
-- B
SELECT nom, prenom, departement, salaire FROM employe WHERE salaire BETWEEN 20 AND 30;
-- 1.11
-- A
SELECT nom, prenom, departement FROM employe WHERE nom LIKE 'B%';
-- B
SELECT nom, prenom, departement FROM employe WHERE nom LIKE 'B%' AND nom LIKE '%e%';
-- 1.12
SELECT nom, prenom FROM employe WHERE nom BETWEEN 'Lebel' AND 'Tremblay';
-- 1.13
SELECT nom FROM employe;
SELECT DISTINCT nom FROM employe;
-- DISTINCT N AFFICHE PAS EN DOUBLE LES EMPLOYE AVEC LE MEME NOM
-- 1.14
SELECT 1;
-- 1.15
SELECT nom, prenom, 1 AS valeur_employe FROM employe WHERE nom BETWEEN 'Lebel' AND 'Tremblay';
-- 1.16
SELECT nom, prenom, salaire, salaire * 0.05 AS "5% du salaire", salaire * 1.05 AS "salaire majoré de 5%" FROM employe;
-- 1.17
SELECT nom, prenom, departement FROM employe WHERE departement NOT IN ('ventes', 'achats');
-- 1.18
SELECT nom, prenom, departement, salaire FROM employe WHERE salaire not BETWEEN  20 AND 30;
-- 1.19
-- A
SELECT nom, prenom, salaire FROM employe WHERE salaire >= 20.0 AND salaire < 30.0;
-- B
SELECT nom, prenom, salaire FROM employe WHERE salaire BETWEEN 20.0 AND 30.0 - 0.01;
-- 1.20 
-- elle est utilisée pour combiner les valeurs des colonnes "prenom" (prénom), "nom" (nom de famille) et "departement" (département) en une seule chaîne.
SELECT CONCAT(prenom, ' ', nom, ' travaille dans le département : ', departement) AS les_employes_et_leur_departement FROM employe;





